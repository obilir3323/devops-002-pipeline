apiVersion: apps/v1
kind: Deployment
metadata:
  name: my-devops-application-deployment
spec:
  selector:
    matchLabels:
      app: my-application
    metadata:
      labels:
        app: my-application
    spec:
      containers:
      - name: my-application
        image: kubobilir/my-application
        resources:
          limits:
            memory: "128Mi"
            cpu: "500m"
        ports:
        - containerPort: <Port>
---
apiVersion: v1
kind: Service
metadata:
  name: my-devops-application-service
spec:
  selector:
    app: my-application
  ports:
  - port: 8080
    targetPort: 8080
